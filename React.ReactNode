import type { Metadata } from 'next'
export const metadata: Metadata = {
  title: 'SMOKE*SCREEN 1.8',
  description: 'Real-Time AI Video Transformation'
}
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className="bg-black text-white min-h-screen m-0">{children}</body>
    </html>
  )
}
